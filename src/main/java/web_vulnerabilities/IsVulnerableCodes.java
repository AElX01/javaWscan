package web_vulnerabilities;

/*
enum sructure to define isVulnerable() method codes necessary to log the right messages to STDOUT
 */

public enum IsVulnerableCodes {
    VULNERABLE,
    SAFE,
    URL_SYNTAX_ERROR,
    REQUEST_PROBLEM
}
